<UserControl x:Class="SchadLucas.EatSmart.UI.Views.TinkerView"
             x:Name="TinkerViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:SchadLucas.EatSmart.ViewModels"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance Type=vm:ITinkerViewModel, IsDesignTimeCreatable=False}">

    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Button Height="50" Width="250"
                Content="Git ApplicationOverlay"
                Background="MediumAquamarine" Foreground="LightSeaGreen" BorderBrush="Aquamarine"
                Command="{Binding OpenOverlayCommand}" />
    </Grid>
</UserControl>